#include <iostream>
#include <algorithm>
#include <string>
#include <vector>
#include <set>

typedef long long int lli;

#define TLE ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);

using namespace std;

int main() {
  TLE;
  lli testCase;
  cin >> testCase;
  while (testCase--) {

  }

  return 0;
}